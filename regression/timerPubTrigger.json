{
    //
    //  This is a scenetest that attempts to touch all parts of the platform.
    //  This includes:
    //      code,
    //      data,
    //      messaging,
    //      timers,
    //      triggers,
    //      user stuff ?? not sure here yet.
    //

    "name": "Name of this test run",
    "scenarios": [ ["Trigger Watcher", 1], ["Create The Timer", 1], ["Subscriber", 1] ],
    "sequencing": "Parallel",

    //  These vars are available to all scenarios by using the "@varname" syntax.
    //  Declare big maps/arrays here so they don't clutter up the scenarios below.
    "vars": {

        //
        // this is the thing that kicks off our Rube Goldberg machine.
        //
        "iterations": 101,
        "uberTimer": {
            "name": "Heartbeat Timer",
            "description": "Publishes to /foo/bar",
            "start_time": "Now",
            "repeats": 100,
            "frequency": 1,
            "service_name": "handlePublish"
        }

    },


    // This guy just starts the process by creating the uber timer.
    "Create The Timer": {
        "name": "Get things going",
        "steps": [
            ["setUser", "foo@bar.com"],
            ["sync", "Ready For Timer", 3],
            ["createTimer", "@uberTimer"]
        ]
    },


    //
    //  Watch for the publish message send by the code service for the timer (huh?
    //
    "Trigger Watcher": {
        "name": "wait for triggers",
        "steps": [
            ["setUser", "bar@bar.com"],
            ["subscribeTriggers"],
            ["sync", "Ready For Timer", 3],
            ["repeat", "@iterations", [
                ["waitTrigger", "Messaging", "Publish", 300]]
            ]
        ]
    },

    //
    //  That timer service also publishes a message. Subscribe to it here
    //
    "Subscriber": {
        "name": "listen for messages",
        "steps": [
            ["setUser", "bar@bar.com"],
            ["subscribe", "/timer/popped", 1],
            ["sync", "Ready For Timer", 3],
            ["repeat", "@iterations", [
                ["waitMessage", "/timer/popped", 300]]
            ]
        ]
    }
}
